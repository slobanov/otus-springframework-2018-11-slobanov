library:
  prompt: "library:>"

spring:
  profiles.active:
      - "mongodb"
      - "rest"
  banner.location: "classpath:/banner.txt"
  liquibase:
    change-log: "classpath:db/changelog/db.changelog-master.xml"

logging.level:
  root: "WARN"

